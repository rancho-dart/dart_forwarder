{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch dart_forwarder",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "env": {
                "GOPATH": "${workspaceFolder}/go"
            },
            "args": [],
            "showLog": true
        }
    ]
}